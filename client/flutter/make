#!/bin/bash

# Wrapper script to support 'make build -r' syntax
if [ "$1" = "build" ] && [ "$2" = "-r" ]; then
    cd /Users/anton/Desktop/client/flutter
    echo "🔨 Building iOS release version..."
    flutter build ios --release
    echo "📱 Installing on iPhone..."
    flutter install --release -d 00008030-001E08892113402E
    echo "✅ Done! App should be on your iPhone home screen."
else
    # Fall back to regular make
    make "$@"
fi

